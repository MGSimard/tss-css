#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../zip-it-and-ship-it/bin.js" "$@"
else
  exec node  "$basedir/../../../zip-it-and-ship-it/bin.js" "$@"
fi
